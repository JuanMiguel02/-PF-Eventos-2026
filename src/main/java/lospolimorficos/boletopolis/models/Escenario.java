package lospolimorficos.boletopolis.models;

/**
 * Record que representa un escenario dentro de un recinto.
 *
 * @param posicion La {@link PosicionEscenario} del escenario dentro del recinto.
 */
public record Escenario(PosicionEscenario posicion) {
}
